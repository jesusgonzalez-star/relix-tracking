================================================================================
                     CHECKLIST RÁPIDO - LUNES 20-04-2026
================================================================================

⏰ TIEMPO: ~6 horas para despliegue completo

================================================================================
MAÑANA 8:00 AM - PREPARAR LA APP (30 MINUTOS)
================================================================================

[ ] 1. Editar .env con datos REALES
    Comando: nano .env
    Cambiar:
      - SQLALCHEMY_DATABASE_URI (usuario/password/BD)
      - SOFTLAND_SERVER, SOFTLAND_USER, SOFTLAND_PASSWORD
      - CORS_ALLOWED_ORIGINS (dominios reales, NO localhost)

[ ] 2. Verificar que DEBUG=False
    Comando: grep DEBUG .env
    Resultado esperado: DEBUG=False

[ ] 3. Git commit de cambios
    Comandos:
      git add app.py utils/db_legacy.py
      git commit -m "🔐 Arreglar SECRET_KEY y SQL Injection PRAGMA"
      git push origin main

[ ] 4. Ejecutar tests
    Comando: python3 -m pytest tests/ -v
    Resultado esperado: todos PASSED

================================================================================
MAÑANA 9:00 AM - PREPARAR SERVIDOR DEBIAN 12 (60 MINUTOS)
================================================================================

[ ] 1. Crear usuario para la app
    sudo useradd -m -s /bin/bash tracking

[ ] 2. Instalar Redis (si no existe)
    sudo apt-get update
    sudo apt-get install redis-server
    sudo systemctl start redis-server
    sudo systemctl enable redis-server

[ ] 3. Crear base de datos MariaDB
    sudo mysql
    > CREATE DATABASE tracking_prod CHARACTER SET utf8mb4;
    > CREATE USER 'tracking_user'@'localhost' IDENTIFIED BY 'password_fuerte';
    > GRANT ALL ON tracking_prod.* TO 'tracking_user'@'localhost';
    > FLUSH PRIVILEGES;
    > EXIT;

[ ] 4. Clonar/copiar código a Debian
    Opción A (Git):
      git clone <repo> /home/tracking/app
    Opción B (SCP):
      scp -r . tracking@debian:/home/tracking/app

[ ] 5. Instalar dependencias
    cd /home/tracking/app
    pip3 install -r requirements.txt

[ ] 6. Copiar .env a servidor
    scp .env tracking@debian:/home/tracking/app/
    ssh tracking@debian "chmod 600 /home/tracking/app/.env"

================================================================================
MAÑANA 10:00 AM - CONFIGURAR APACHE (60 MINUTOS)
================================================================================

[ ] 1. Habilitar módulos Apache
    sudo a2enmod proxy
    sudo a2enmod proxy_http
    sudo a2enmod ssl
    sudo a2enmod headers

[ ] 2. Crear VirtualHost Apache
    sudo nano /etc/apache2/sites-available/tracking.conf

    Copiar contenido:
    ──────────────────────────────────────────────────────────────────────
    <VirtualHost *:80>
        ServerName tracking.tudominio.cl
        ServerAlias www.tracking.tudominio.cl

        DocumentRoot /home/tracking/app

        ProxyPreserveHost On
        ProxyPass / http://127.0.0.1:8000/
        ProxyPassReverse / http://127.0.0.1:8000/

        <Directory /home/tracking/app>
            Allow from all
        </Directory>

        ErrorLog ${APACHE_LOG_DIR}/tracking_error.log
        CustomLog ${APACHE_LOG_DIR}/tracking_access.log combined
    </VirtualHost>
    ──────────────────────────────────────────────────────────────────────

[ ] 3. Habilitar sitio
    sudo a2ensite tracking
    sudo apache2ctl configtest  # Debe salir: Syntax OK
    sudo systemctl restart apache2

[ ] 4. Crear systemd service
    sudo nano /etc/systemd/system/tracking.service

    Copiar contenido:
    ──────────────────────────────────────────────────────────────────────
    [Unit]
    Description=Tracking Logístico
    After=network.target

    [Service]
    Type=notify
    User=tracking
    WorkingDirectory=/home/tracking/app
    Environment="PATH=/usr/local/bin:/usr/bin"
    ExecStart=/usr/bin/python3 -m gunicorn -c gunicorn_config.py app:create_app()
    Restart=always
    RestartSec=10

    [Install]
    WantedBy=multi-user.target
    ──────────────────────────────────────────────────────────────────────

[ ] 5. Iniciar servicio
    sudo systemctl daemon-reload
    sudo systemctl start tracking
    sudo systemctl enable tracking
    sudo systemctl status tracking  # Debe salir: active (running)

================================================================================
MAÑANA 11:00 AM - VERIFICAR DESPLIEGUE (30 MINUTOS)
================================================================================

[ ] 1. Verificar app está corriendo
    curl http://localhost:8000/health
    Resultado esperado: {"status": "healthy"}

[ ] 2. Verificar /status endpoint
    curl http://localhost:8000/status
    Debe mostrar: DEBUG=False, base de datos OK

[ ] 3. Verificar logs
    sudo journalctl -u tracking -f
    No debe haber errores CRÍTICOS

[ ] 4. Prueba de login
    Abrir en navegador: http://tracking.tudominio.cl
    Intentar login con usuario de prueba
    Resultado esperado: Dashboard carga sin errores

[ ] 5. Prueba de importación OC (BODEGA)
    Login como usuario BODEGA
    Importar OC de prueba desde Softland
    Resultado esperado: OC aparece con estado INGRESADO

[ ] 6. Prueba de recepción en faena (FAENA)
    Login como usuario FAENA
    Recibir OC con foto
    Resultado esperado: Estado cambia a ENTREGADO, foto guarda

================================================================================
MAÑANA 14:00 (2:00 PM) - VALIDACIÓN FINAL (30 MINUTOS)
================================================================================

[ ] 1. Revisar logs producción
    sudo tail -f /var/log/apache2/tracking_access.log
    sudo tail -f /var/log/apache2/tracking_error.log
    sudo journalctl -u tracking -f
    No deben haber errores

[ ] 2. Verificar seguridad
    ✅ SECRET_KEY está en variables de entorno (no en código)
    ✅ .env tiene contraseñas, no en Git
    ✅ DEBUG=False en .env
    ✅ SESSION_COOKIE_SECURE=True
    ✅ RATELIMIT_ENABLED=True
    ✅ Redis está corriendo

[ ] 3. Verificar performance
    curl http://tracking.tudominio.cl/health
    Tiempo de respuesta: <500ms

[ ] 4. Verificar backups
    Backup de BD: /var/backups/tracking_prod_20260420.sql
    Comando: mysqldump -u tracking_user -p tracking_prod > backup.sql

[ ] 5. Documentar en Jira/Linear
    - Vulnerabilidades CRÍTICAS arregladas: 2/2 ✅
    - Tests pasados: 11/11 ✅
    - Despliegue exitoso: ✅
    - Fecha: 2026-04-20
    - Hora: 14:00

================================================================================
VARIABLES DE ENTORNO CONFIRMADAS (en .env)
================================================================================

✅ SECRET_KEY=b605dc8ce39dc225c556c5fdbd028a862ca2ffd486e626a0f3261b42b04b2961
✅ API_SECRET=4f9e5dbf1509e4bfad6c72f8aae584488826384cfb20c02a7635c851541099ef
✅ SQLALCHEMY_DATABASE_URI=mysql+pymysql://tracking_user:PASSWORD@localhost:3306/tracking_prod
✅ DEBUG=False
✅ RATELIMIT_ENABLED=True
✅ RATELIMIT_STORAGE_URI=redis://localhost:6379/0
✅ SESSION_COOKIE_SECURE=True
✅ ENABLE_SWAGGER=False
✅ BEHIND_PROXY=True

================================================================================
🆘 SI ALGO FALLA
================================================================================

1. ERROR: "SECRET_KEY no está configurada"
   → Asegúrate que .env existe en la raíz del proyecto
   → Verifica: grep SECRET_KEY .env

2. ERROR: "Redis connection refused"
   → Instalar: sudo apt-get install redis-server
   → Iniciar: sudo systemctl start redis-server

3. ERROR: "Database connection refused"
   → Crear usuario MariaDB con permisos
   → Verificar: mysql -u tracking_user -p

4. ERROR: "Module not found"
   → Reinstalar dependencias: pip3 install -r requirements.txt

5. CONSULTAR TROUBLESHOOTING_LUNES.md (tiene 10 soluciones)

================================================================================
CONTACTOS IMPORTANTES
================================================================================

Usuario: jesus.gonzalez@relixwater.com
Servidor Debian: [IP a definir]
Servidor Softland: [IP a definir]

Crisis/Emergencia: Rollback inmediato
  1. sudo systemctl stop tracking
  2. git revert HEAD
  3. Restaurar backup BD

================================================================================
ESTADO FINAL ESPERADO
================================================================================

✅ 2 vulnerabilidades CRÍTICAS arregladas
✅ Tests pasando (11/11)
✅ Despliegue en Debian 12 exitoso
✅ Apache + MariaDB + Redis corriendo
✅ App accesible en https://tracking.tudominio.cl
✅ Flujos de negocio funcionando (importar, recibir, entregar)
✅ Logs limpios sin errores

Score de seguridad: 65/100 (mejorado de 45/100)

================================================================================
Generado: 2026-04-19 19:50
Por: Claude Haiku 4.5
================================================================================
